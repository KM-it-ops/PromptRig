# MISSION-035 Hosted Simple+Developer Slice Runtime

**Status:** OAR-028 Ready (not Accepted).
**Baseline:** MISSION-034 contracts (`d9b1398`). Skip-cert law is OAR-022 Ready (not undone).
**Q2 pick:** `STACK-OWNER-SELECTED` — stdlib `HostedSlice` file store and JSON Simple/Developer views. **Not FastAPI. Not Next.js.**

This mission is **not CERTIFIED**. The requirements compiler stays **PARTIAL**.

## What this mission records (narrow)

- **OAR-028 Ready (not Accepted).**
- One hosted slice: Simple Mode and Developer Mode show the same `ir_sha256` as `closed-loop` for one project.
- Intake is structured / `plain_language_v0` / 008 bridge. `simple_mode_ui` stays forbidden.
- `apps/dashboard` and `apps/promptrig.jsx` are unused.
- Export then delete: the compiler cannot see the deleted project. Cross-tenant read fails closed.
- **Not a live** default path. `execute-openai` stays opt-in.
- Skip-cert law (OAR-022) is not undone.
- OAR-027 remains MISSION-034 contracts (not rewritten, not Accepted).

## Non-claims

- Not CERTIFIED requirements compiler. Not full MISSION-008. Not full Phase 4B exit.
- Not **M3** / Simple Mode UI-only semantics.
- Not FastAPI. Not Next.js. Not CERTIFIED hosted product.
- **Not a live** provider path on the default slice.
