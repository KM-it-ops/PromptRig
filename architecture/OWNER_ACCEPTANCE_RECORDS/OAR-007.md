# OAR-007 — MISSION-013 Plain-Language M1 (Constrained Prose) Acceptance

**Status:** Ready for owner acceptance. **Not Accepted** — Boss has not signed.

**Certified if accepted:** headless offline deterministic `plain_language_v0` constrained grammar intake (`plain_language.py`) that emits `structured_minimal_v0` records, then runs the existing MISSION-012 closed loop (`closed_loop_from_json` / `run_closed_loop`) with `intake_profile` evidence — fake adapter only, `network_allowed=false`, repair budgets `{0,1,2}`, `EVR-SEC-0001`, test hooks quarantined behind `ClosedLoopTestHooks`. Parse failures fail closed with `PL-PARSE-*` diagnostics; freeform prose never becomes IR.

**Still unauthorized without new campaign:** freeform NLP, M2 model-assisted suggestion (MISSION-014), M3 / Simple Mode UI semantics, live providers, full MISSION-008 production requirements compiler, full Roadmap Phase 4B exit, benchmarks/claims, hosted product surfaces, MissionRig, IR v0.2, enterprise SAST, full external-consumer conformance matrix, performance/resource ceiling proof. Requirements compiler maturity remains **`PARTIAL`** even after acceptance.
