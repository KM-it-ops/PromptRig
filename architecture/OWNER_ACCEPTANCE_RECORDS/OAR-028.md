# OAR-028 — MISSION-035 Hosted Simple+Developer Slice Runtime

**Status:** Ready (not Accepted).

This record is Ready and is not Accepted until Boss Accepts it. Do not treat Ready as Accepted.

**Certified if accepted:** a narrow hosted slice exists (`hosted_slice.py`) where Simple Mode and Developer Mode views of one file-stored project share the same `ir_sha256` as headless `closed-loop`. Q2 pick is **STACK-OWNER-SELECTED** (stdlib dispatch + JSON views). **Not FastAPI. Not Next.js.** `apps/dashboard` and `apps/promptrig.jsx` unused. `simple_mode_ui` remains forbidden. Export/deletion and cross-tenant fail-closed are implemented. This is **not CERTIFIED** hosted product. Requirements compiler stays **PARTIAL**. Fake-adapter oracle stays CERTIFIED. Skip-cert law (OAR-022) is not undone.

**Still unauthorized without new campaign:** FastAPI/Next.js restack, M3 UI-only semantics, CERTIFIED hosted product, CERTIFIED compiler promotion, live on the default hosted path, MissionRig beyond MISSION-036, enterprise SAST. OAR-021 through OAR-027 remain Ready sibling records (not rewritten, not Accepted by this mission).
