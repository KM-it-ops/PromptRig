# Philosophy

Architecture precedes implementation. Contracts are more durable than code. Evidence outranks preference. Historical artifacts remain immutable. Unsupported semantics are never silently ignored.
