# Architectural Laws

1. Architecture precedes implementation.
2. Validation occurs before lowering.
3. Lowering never mutates canonical IR.
4. Diagnostics are immutable.
5. Optimization preserves intent.
6. Adapters never silently discard semantics.
7. Historical artifacts are immutable.
